{
  "name": "social-receipt",
  "version": "1.0.0",
  "private": true,
  "description": "Social Receipt static app + Netlify functions",
  "scripts": {
    "validate": "node tests/validate.js",
    "test:e2e": "playwright test",
    "test": "npm run validate && npm run test:e2e",
    "serve": "node tests/server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
